<<<<<<< HEAD
<%- include('partials/header') %>
<body class="bg-gray-100">
  <main class="container mx-auto p-4">
    <h1 class="text-center text-3xl mb-4">Mijn Profiel</h1>
    <div class="profile-box">
      <h2 class="text-2xl mb-4">Gebruikersinformatie</h2>
      <form action="/profile/update" method="POST" class="mb-4">
        <div class="mb-4">
          <label for="username" class="block text-lg font-medium text-gray-700"
            >Gebruikersnaam:</label
=======
<!DOCTYPE html>
<html lang="nl-BE">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Profielpagina van de gebruiker waar je informatie kunt bijwerken en een hoofd-Pokémon kunt kiezen."
    />
    <meta
      name="keywords"
      content="Pokémon, profiel, bijwerken, hoofd-Pokémon, AP Hogeschool, project"
    />
    <meta
      name="author"
      content="Cedric Demaeschalk - Ian Mondelaers - Jesus O'Bulan"
    />
    <title>Mijn Profiel - Mew.Tech</title>
    <link rel="icon" type="image/x-icon" href="/assets/images/logo.webp" />
    <link
      href="https://unpkg.com/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      href="https://unpkg.com/flowbite@1.5.1/dist/flowbite.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f0f0f0;
        background-image: url("../assets/images/grassland.webp");
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        font-family: "Press Start 2P", cursive;
        padding: 1rem;
        min-height: 100vh;
      }
      .pixel-border {
        border: 4px solid #444;
        box-shadow: 4px 4px 0px #aaa;
      }
      .pixel-button {
        background-color: #444;
        color: #fff;
        border: 2px solid #222;
        text-shadow: 1px 1px 0px #000;
      }
      .pixel-button:hover {
        background-color: #555;
      }
      .pixel-button-pokemon {
        background-color: #ffcc00; /* Pokémon yellow */
        color: #fff;
        border: 2px solid #cc0000; /* Pokémon red */
        text-shadow: 1px 1px 0px #000;
        text-align: center;
      }
      .pixel-button-pokemon:hover {
        background-color: #ffdd00;
      }
      .pokemon-img {
        width: 100px;
        height: 100px;
        object-fit: contain;
        animation: appear 1s forwards;
      }
      .pokemon-box {
        background-color: rgba(255, 255, 255, 0.221);
        border: 4px solid #444;
        box-shadow: 4px 4px 0px #aaa;
        padding: 10px;
        text-align: center;
        margin: 0 auto;
        cursor: pointer;
        height: 180px; /* Fixed height for uniformity */
      }
      .pokemon-name {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        width: 100px; /* Ensure the text truncates within the box */
        margin: 0 auto;
      }
      .profile-box {
        background-color: rgba(255, 255, 255, 0.221);
        border: 4px solid #444;
        box-shadow: 4px 4px 0px #aaa;
        padding: 20px;
        text-align: center;
        margin-right: 20px; /* Add margin to the right */
        width: 100%; /* Set fixed width for profile box */
        max-width: 250px; /* Maximum width for large screens */
      }
      .caught-pokemon-box {
        background-color: rgba(255, 255, 255, 0.221);
        border: 4px solid #444;
        box-shadow: 4px 4px 0px #aaa;
        padding: 20px;
        text-align: center;
        flex-grow: 1; /* Allow it to fill the remaining space */
      }
      .pokemon-grid {
        display: grid;
        gap: 10px;
      }
      .highlighted {
        border-color: #00ff00 !important; /* Highlight border color */
      }
      @keyframes appear {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }
      header {
        background: linear-gradient(to right, #ff0000, #ffcc00);
        color: white;
        text-align: center;
        padding: 10px;
        border-radius: 10px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.5);
      }

      /* Add this CSS to style the selected Pokémon */
      .selected-pokemon {
        border-color: #00ff00; /* Green border for selected Pokémon */
      }

      /* Custom media queries for responsive grid */
      .pokemon-grid {
        grid-template-columns: repeat(2, 1fr); /* Default: 2 columns */
      }
      @media (min-width: 640px) {
        .pokemon-grid {
          grid-template-columns: repeat(2, 1fr); /* Small screens: 2 columns */
        }
      }
      @media (min-width: 768px) {
        .pokemon-grid {
          grid-template-columns: repeat(4, 1fr); /* Medium screens: 4 columns */
        }
      }
      @media (min-width: 1024px) {
        .pokemon-grid {
          grid-template-columns: repeat(4, 1fr); /* Large screens: 4 columns */
        }
      }
      @media (min-width: 1280px) {
        .pokemon-grid {
          grid-template-columns: repeat(
            6,
            1fr
          ); /* Extra-large screens: 6 columns */
        }
      }
      @media (min-width: 1536px) {
        .pokemon-grid {
          grid-template-columns: repeat(8, 1fr); /* 2XL screens: 8 columns */
        }
      }
    </style>
  </head>
  <body class="bg-gray-100">
    <header class="pixel-border p-4 mb-4">
      <nav
        class="navbar container mx-auto flex flex-col sm:flex-row items-center px-4"
      >
        <div class="flex justify-between items-center w-full sm:w-auto">
          <a class="text-2xl font-bold flex items-center" href="/main">
            <img
              src="./assets/images/logo.webp"
              alt="logo"
              class="h-10 w-10 mr-2"
            />
          </a>
          <button
            data-collapse-toggle="navbar-default"
            type="button"
            class="inline-flex items-center p-2 text-sm text-gray-500 rounded-lg lg:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-gray-400 dark:hover:bg-gray-700 dark:focus:ring-gray-600"
            aria-controls="navbar-default"
            aria-expanded="false"
>>>>>>> production
          >
          <input
            type="text"
            class="form-input mt-1 block w-full"
            id="username"
            name="username"
            value="JohnDoe"
          />
        </div>
        <div class="mb-4">
          <label for="email" class="block text-lg font-medium text-gray-700"
            >E-mail:</label
          >
          <input
            type="email"
            class="form-input mt-1 block w-full"
            id="email"
            name="email"
            value="john@example.com"
          />
        </div>
        <button type="submit" class="pixel-button-pokemon py-2 px-4 rounded">
          Informatie opslaan
        </button>
      </form>

      <h2 class="text-2xl mb-4">Kies je hoofd-Pokémon</h2>
      <form action="/profile/set-main-pokemon" method="POST">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
          <% const caughtPokemons = [ { id: 1, name: "Pikachu", sprite:
          "/assets/images/pikachu.webp" }, { id: 2, name: "Charmander", sprite:
          "/assets/images/charmander.webp" }, { id: 3, name: "Squirtle", sprite:
          "/assets/images/squirtle.webp" }, { id: 4, name: "Bulbasaur", sprite:
          "/assets/images/bulbasaur.webp" } ]; %> <%
          caughtPokemons.forEach(pokemon => { %>
          <div class="pokemon-box">
            <label class="cursor-pointer">
              <input
                type="radio"
                name="mainPokemon"
                value="<%= pokemon.id %>"
                class="hidden"
              />
              <img
                src="<%= pokemon.sprite %>"
                alt="<%= pokemon.name %>"
                class="pokemon-img mx-auto mb-2"
              />
              <p class="text-xl"><%= pokemon.name %></p>
            </label>
          </div>
          <% }); %>
        </div>
        <button
          type="submit"
          class="pixel-button-pokemon py-2 px-4 rounded mt-4"
        >
          Hoofd-Pokémon instellen
        </button>
      </form>
    </div>
  </main>

<<<<<<< HEAD
  <script src="https://unpkg.com/flowbite@1.5.1/dist/flowbite.js"></script>
</body>
=======
    <main class="container mx-auto p-4 flex flex-col lg:flex-row lg:space-x-4">
      <div class="profile-box w-full lg:w-1/5 mb-4 lg:mb-0">
        <h3 class="text-md mb-4">info</h3>
        <form action="/viewer" method="POST" class="mb-4">
          <div class="mb-4">
            <label
              for="username"
              class="block text-sm font-medium text-gray-700"
              >Gebruikersnaam:</label
            >
            <input
              type="text"
              class="form-input mt-1 block w-full"
              id="username"
              name="username"
              value="<%= user.username %>"
            />
          </div>
          <div class="mb-4">
            <label for="email" class="block text-sm font-medium text-gray-700"
              >E-mail:</label
            >
            <input
              type="email"
              class="form-input mt-1 block w-full"
              id="email"
              name="email"
              value="<%= user.email %>"
            />
          </div>
          <button
            type="submit"
            class="pixel-button-pokemon text-sm py-2 px-4 rounded"
          >
            Informatie opslaan
          </button>
        </form>
        <h3 class="text-md mb-4">Geselecteerde Hoofd-Pokémon</h3>
        <% if (selectedPokemon) { %>
        <div class="pokemon-box">
          <img
            src="<%= selectedPokemon.sprites.front_default %>"
            alt="<%= selectedPokemon.name %>"
            class="pokemon-img mx-auto mb-2"
          />
          <p class="text-xl"><%= selectedPokemon.name %></p>
        </div>
        <% } else { %>
        <p>Geen hoofd-Pokémon geselecteerd</p>
        <% } %>
      </div>
      <div class="caught-pokemon-box flex-grow">
        <h3 class="text-md mb-4">Kies je hoofd-Pokémon</h3>
        <form
          id="pokemonForm"
          action="/viewer/set-main-pokemon"
          method="POST"
          class="flex-grow"
        >
          <div class="pokemon-grid">
            <% caughtPokemon.forEach(pokemon => { %>
            <div class="pokemon-box" data-pokemon-id="<%= pokemon.id %>">
              <label class="cursor-pointer">
                <input
                  type="radio"
                  name="mainPokemon"
                  value="<%= pokemon.id %>"
                  class="hidden"
                />
                <a href="/pokeDex/<%= pokemon.id %>">
                  <img
                    src="<%= pokemon.sprites.front_default %>"
                    alt="<%= pokemon.name %>"
                    class="pokemon-img mx-auto mb-2"
                  />
                </a>
                <p class="text-sm pokemon-name"><%= pokemon.name %></p>
              </label>
            </div>
            <% }); %>
          </div>
          <button
            type="submit"
            class="pixel-button-pokemon py-2 px-4 rounded mt-4"
          >
            Hoofd-Pokémon instellen
          </button>
        </form>
      </div>
    </main>

    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const pokemonBoxes = document.querySelectorAll(".pokemon-box");

        pokemonBoxes.forEach((box) => {
          box.addEventListener("click", function () {
            // Remove highlight from all boxes
            pokemonBoxes.forEach((b) => b.classList.remove("highlighted"));
            // Add highlight to the clicked box
            box.classList.add("highlighted");
            // Check the corresponding radio button
            box.querySelector("input[type='radio']").checked = true;
          });
        });
      });
    </script>
  </body>
</html>
>>>>>>> production

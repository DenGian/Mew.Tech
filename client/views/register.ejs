<!DOCTYPE html>
<html lang="nl-BE">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://unpkg.com/tailwindcss@2.2.19/dist/tailwind.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f0f0f0;
        background-image: url("../assets/images/grassland.webp");
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        font-family: "Press Start 2P", cursive;
        height: 100vh; /* Full height of viewport */
        display: flex;
        align-items: center; /* Center vertically */
        justify-content: center; /* Center horizontally */
        cursor: url("../assets/images/pokeball_cursor.png"), auto; /* Custom cursor */
      }
      .main {
        animation: fadeIn 1s ease-out forwards;
        background: rgba(
          255,
          255,
          255,
          0.9
        ); /* Slightly more opaque for better readability */
        border-radius: 10px;
        box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.3);
        padding: 20px;
        width: 100%;
        max-width: 400px; /* Better control over form width */
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: scale(0.95);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }
      .pixel-button {
        background-color: #ffcc00; /* Pokémon yellow */
        color: #fff;
        border: 3px solid #cc0000; /* Pokémon red */
        text-shadow: 1px 1px 0px #000;
        font-family: "Press Start 2P";
        padding: 8px 0; /* Better padding */
        transition: background-color 0.3s ease;
        cursor: pointer; /* Ensure cursor changes on hover */
      }
      .pixel-button:hover {
        background-color: #ffdd00;
      }
      .form-control {
        background-color: #ffcc00; /* Pokémon yellow */
        border: 2px solid #cc0000; /* Pokémon red */
        color: #000;
        font-family: "Press Start 2P";
        padding: 8px; /* More padding for better text visibility */
      }
      .form-label {
        color: #000;
        font-family: "Press Start 2P";
      }
      .form-group {
        margin-bottom: 1rem;
      }
      .back-link {
        background-color: #4d96ff; /* Blue for contrast */
        border: 2px solid #0041c2; /* Darker blue border */
        display: block;
        text-align: center;
        margin-top: 12px;
        transition: background-color 0.3s ease;
      }
      .back-link:hover {
        background-color: #6ba4ff; /* Lighter blue on hover */
      }
    </style>
    <title>Mew.Tech - Registratie</title>
  </head>
  <body class="landing-page">
    <div class="main">
      <h2 class="text-center font-bold mb-4">
        Welkom bij onze Registratie pagina
      </h2>
      <% if (message) { %>
      <p class="<%= message.type %>"><%= message.message %></p>
      <% } %>
      <form action="/register" method="POST">
        <div class="form-group">
          <label for="email" class="form-label">Email</label>
          <input
            type="email"
            class="form-control"
            id="email"
            name="email"
            required
          />
        </div>
        <div class="form-group">
          <label for="username" class="form-label">Username</label>
          <input
            type="text"
            class="form-control"
            id="username"
            name="username"
            required
          />
        </div>
        <div class="form-group">
          <label for="password" class="form-label">Password</label>
          <input
            type="password"
            class="form-control"
            id="password"
            name="password"
            required
          />
        </div>
        <button type="submit" class="w-full pixel-button">Registreer</button>
      </form>
      <a href="/login" class="pixel-button back-link">Terug naar Login</a>
      <!-- Back to Login link -->
    </div>
  </body>
</html>
